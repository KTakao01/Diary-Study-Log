# paiza
B138:ドーナツ
https://paiza.jp/en_try/challenges/679

## 正答（自身の回答例）


```js
process.stdin.resume();
process.stdin.setEncoding('utf8');
// 自分の得意な言語で
// Let's チャレンジ！！
var lines = [];
var reader = require('readline').createInterface({
input: process.stdin,
output: process.stdout
});
reader.on('line', (line) => {
lines.push(line);
});
reader.on('close', () => {
const [H, W] = lines[0].trim().split(" ").map(Number);
const groundArt = Array.from({length: H}, () => Array(W).fill(false));
// console.log(groundArt);
// 地上絵を取得
for(let i = 0; i < H; i++){
    let hArr = [];
    hArr = lines[i+1].split("");
    for(let j=0; j < W; j++){
        groundArt[i][j] = hArr[j]
    }
}
 
// console.log(groundArt);
let ans = 0;
// ドーナツ絵の判定プログラム
for(let i = 0; i < H - 2; i++){
    for(let j = 0; j < W - 2 ; j++){
 
        // if (groundArt[i][j] != "#" || groundArt[i][j+1] != "#" || groundArt[i][j+2] != "#"){
        //     continue;
        // }
        // if (groundArt[i+1][j] != "#" || groundArt[i+1][j+1] != "." || groundArt[i+1][j+2] != "#"){
        //     continue;
        // }
        // if (groundArt[i+2][j] != "#" || groundArt[i+2][j+1] != "#" || groundArt[i+2][j+2] != "#"){
        //     continue;
        // }
            // ドーナツ絵の条件
        const isDonut = groundArt[i][j] == "#" && groundArt[i][j+1] == "#" && groundArt[i][j+2] == "#" &&
                groundArt[i+1][j] == "#" && groundArt[i+1][j+1] == "." && groundArt[i+1][j+2] == "#"&&
                groundArt[i+2][j] == "#" && groundArt[i+2][j+1] == "#" && groundArt[i+2][j+2] == "#";
 
        if (isDonut){
            ans += 1;
        }
        
        // ans += 1;
    }
}
 
console.log(ans)
 
});
// ### 横一列# 左端をi,j
// #.# #    穴は、i+1,j+1 左端はi+1,j
// ###   横一列# 左端はi+2,j
// 地上絵
// ドーナツがあるかどうかの確認
// あれば実際にtrueをおいて重複数え上げを防ぐ
// ドーナツがあるかどうかの確認
// あれば実際にtrueをおいて重複数え上げを防ぐ
// ループ・・・
// ドーナツがあるかどうか確認するプログラム・・・
// ドーナツ座標9個に対して記号が一致するかどうかの確認をすれば良い
// ドーナツがあれば左上端の座標さえわかれば,ドーナツ絵の位置が全てわかる。
// 上限H W 最下行がi+2<= H , 最右列がj+2<=W


```