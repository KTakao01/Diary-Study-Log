# paiza B
【異能コラボ問題】B131:運賃計算
https://paiza.jp/career/challenges/621/retry
## 正答（自身の回答）
```js
process.stdin.resume();
process.stdin.setEncoding('utf8');
// 自分の得意な言語で
// Let's チャレンジ！！
var lines = [];
var reader = require('readline').createInterface({
  input: process.stdin,
  output: process.stdout
});
reader.on('line', (line) => {
  lines.push(line);
});
reader.on('close', () => {
　let currentLine = 0; 
  const [N,M] = lines[currentLine++].trim().split(" ").map(Number);
  let station = [];
  for (let i = 0; i < N; i++){
      let arr = lines[currentLine++].trim().split(" ").map(Number);
       station.push(arr);
  }
//   console.log(station);
    let price = 0;
    let currentY = 0;
    let currentX = 0;
    const X = lines[currentLine++].trim().split(" ").map(Number);
    for (let i = 0; i < X; i++){
        let pos = lines[currentLine++].trim().split(" ").map(Number);
        let [y,x] = [pos[0]-1,pos[1]-1];
        // console.log(y,x)
        if (y != currentY && x != currentX){
            currentY = y;
            curretX = x;
            // console.log("乗り換え",currentY,currentX);
            // console.log("0banb",y,x,price);
        }
        if (x > currentX){
            price += station[y][x] - station[currentY][currentX];
            // console.log("1banb",y,x,price);
            currentY = y;
            currentX = x;
            // console.log("pos",currentY,currentX);

        } 
        if (x < currentX){
            price += station[currentY][currentX] - station[y][x];
            // console.log("2banb",y,x,price);
            currentY = y;
            currentX = x;
            // console.log("pos",currentY,currentX);
        }
    }
    console.log(price);
  
});

// 3 4
// 0 1 2 3
// 0 4 5 6
// 0 7 8 9
// 3
// 1 4 // 現在地（1,1)とおく　目的地の列＞現在地の列：順方向
// 3 2 // 行が変わるたびに現在地更新（1,4)とおく　目的地の行ではない、列固定して移動 (3,4)に更新
　　　// 目的地の列＜現在地の列：逆方向
// 2 2

// 行変更　列固定　→ 無料
// 逆方向：現在地の価格-目的地の価格
// 順方向：目的地の価格-現在地の価格
```

## 改善点
・絶対値の利用
・現在地は記録する必要がない。目的地と現在地の行が異なるときは、0円で乗り換え。0円で乗り換えを表現する必要はない。